# 秒杀系统实际测试指南

**测试时间：** 2026-02-08  
**测试目标：** 验证秒杀系统功能完整性和性能表现  
**预计耗时：** 30-45分钟

---

## 📋 测试前准备清单

### ✅ 第一步：环境检查（5分钟）

#### 1.1 检查MySQL是否启动
```bash
# 方式1：命令行检查
mysql -uroot -proot -e "SELECT 1"

# 方式2：查看进程
tasklist | findstr mysql

# 预期结果：能正常连接，返回 1
```

**如果MySQL未启动：**
```bash
# 启动MySQL服务
net start mysql
# 或者启动MySQL安装目录下的mysqld.exe
```

---

#### 1.2 检查Redis是否启动
```bash
# 方式1：命令行检查
redis-cli ping

# 方式2：查看进程
tasklist | findstr redis

# 预期结果：返回 PONG
```

**如果Redis未启动：**
```bash
# 启动Redis服务
redis-server

# 或者启动Redis安装目录下的redis-server.exe
```

---

#### 1.3 检查RocketMQ是否启动
```bash
# 检查NameServer端口（9876）
netstat -ano | findstr 9876

# 检查Broker端口（10911）
netstat -ano | findstr 10911

# 预期结果：两个端口都在监听
```

**如果RocketMQ未启动：**
```bash
# 1. 启动NameServer
cd D:\rocketmq\bin
start mqnamesrv.cmd

# 2. 启动Broker
start mqbroker.cmd -n 127.0.0.1:9876 autoCreateTopicEnable=true
```

---

#### 1.4 检查Java环境
```bash
# 检查Java版本
java -version

# 预期结果：Java 1.8 或更高版本
```

---

#### 1.5 检查Maven环境
```bash
# 检查Maven版本
mvn -version

# 预期结果：Maven 3.x
```

---

### ✅ 第二步：初始化数据库（2分钟）

```bash
# 进入项目目录
cd D:\code\cloud-demo\cloud-demo\seckill-demo

# 执行初始化脚本
mysql -uroot -proot < init.sql
```

**验证数据库初始化：**
```sql
-- 登录MySQL
mysql -uroot -proot

-- 切换数据库
USE cloud_seckill;

-- 查看表
SHOW TABLES;

-- 查看秒杀商品
SELECT id, product_name, stock, price FROM tb_seckill_product;

-- 预期结果：
-- +----+----------------------+-------+---------+
-- | id | product_name         | stock | price   |
-- +----+----------------------+-------+---------+
-- |  1 | iPhone 15 Pro 256GB  | 10000 | 6999.00 |
-- |  2 | MacBook Pro 14寸     |  5000 | 11999.00|
-- |  3 | AirPods Pro 2代      | 20000 | 1499.00 |
-- +----+----------------------+-------+---------+
```

---

## 🚀 测试执行步骤

### ✅ 第三步：启动服务（5分钟）

#### 方式1：使用快速启动脚本（推荐）
```bash
# 双击运行
快速启动脚本.bat
```

#### 方式2：手动启动
```bash
# 终端1：启动seckill-service
cd seckill-service
mvn spring-boot:run

# 终端2：启动seckill-consumer
cd seckill-consumer
mvn spring-boot:run
```

**等待服务启动完成（约30-60秒）**

---

#### 3.1 验证seckill-service启动成功
**查看日志输出：**
```
========================================
秒杀服务启动成功！端口：8092
========================================
```

**测试接口可访问：**
```bash
# 方式1：浏览器访问
http://localhost:8092

# 方式2：curl测试
curl http://localhost:8092
```

---

#### 3.2 验证seckill-consumer启动成功
**查看日志输出：**
```
========================================
秒杀消费者启动成功！端口：8093
========================================
```

---

### ✅ 第四步：功能测试（10分钟）

#### 测试1：预热库存 ⭐核心测试
**目的：** 将MySQL库存加载到Redis

**执行命令：**
```bash
curl -X POST http://localhost:8092/seckill/preload/1
```

**预期结果：**
```json
{
  "success": true,
  "message": "库存预热成功",
  "data": null
}
```

**验证Redis库存：**
```bash
redis-cli GET seckill:stock:1

# 预期结果：10000
```

**查看日志：**
```
预热库存成功：商品ID=1，库存=10000
```

---

#### 测试2：秒杀请求 ⭐核心测试
**目的：** 验证秒杀流程是否正常

**执行命令：**
```bash
curl -X POST http://localhost:8092/seckill/kill ^
  -H "Content-Type: application/json" ^
  -d "{\"userId\":1001,\"productId\":1,\"quantity\":1}"
```

**预期结果：**
```json
{
  "success": true,
  "message": "秒杀成功，请等待支付",
  "data": "SK1707379200000100011"
}
```

**查看seckill-service日志：**
```
========== 秒杀请求 ==========
用户ID：1001，商品ID：1，数量：1
Redis扣减库存结果：9999
秒杀成功，发送MQ消息
秒杀成功，订单号：SK1707379200000100011
==================================
```

**查看seckill-consumer日志：**
```
========== 秒杀订单消费 ==========
收到秒杀消息：用户ID=1001, 商品ID=1, 数量=1
MySQL库存扣减成功：商品ID=1, 数量=1
订单创建成功：订单号=SK1707379200000100011
秒杀订单处理完成：订单号=SK1707379200000100011
==================================
```

---

#### 测试3：重复秒杀 ⭐防重复测试
**目的：** 验证防重复秒杀机制

**执行命令：**
```bash
# 再次用同一用户秒杀同一商品
curl -X POST http://localhost:8092/seckill/kill ^
  -H "Content-Type: application/json" ^
  -d "{\"userId\":1001,\"productId\":1,\"quantity\":1}"
```

**预期结果：**
```json
{
  "success": false,
  "message": "秒杀失败，库存不足或已秒杀",
  "data": null
}
```

**查看日志：**
```
用户1001已秒杀商品1，不能重复秒杀
```

---

#### 测试4：库存不足 ⭐防超卖测试
**目的：** 验证库存不足时的处理

**准备工作：**
```bash
# 将Redis库存设置为0
redis-cli SET seckill:stock:2 0
```

**执行命令：**
```bash
curl -X POST http://localhost:8092/seckill/kill ^
  -H "Content-Type: application/json" ^
  -d "{\"userId\":2001,\"productId\":2,\"quantity\":1}"
```

**预期结果：**
```json
{
  "success": false,
  "message": "秒杀失败，库存不足或已秒杀",
  "data": null
}
```

**查看日志：**
```
Redis扣减库存结果：0
商品2库存不足
```

---

#### 测试5：并发秒杀 ⭐并发测试
**目的：** 验证并发场景下的正确性

**执行命令：**
```bash
# 预热库存
curl -X POST http://localhost:8092/seckill/preload/3

# 同时发起10个秒杀请求（不同用户）
for /L %i in (3001,1,3010) do (
  start /B curl -X POST http://localhost:8092/seckill/kill ^
    -H "Content-Type: application/json" ^
    -d "{\"userId\":%i,\"productId\":3,\"quantity\":1}"
)
```

**预期结果：**
- 10个请求都应该成功
- Redis库存应该减少10
- MySQL库存应该减少10
- 创建10条订单

**验证：**
```bash
# 查看Redis库存
redis-cli GET seckill:stock:3

# 查看MySQL库存
mysql -uroot -proot cloud_seckill -e "SELECT stock FROM tb_seckill_product WHERE id=3"

# 查看订单数量
mysql -uroot -proot cloud_seckill -e "SELECT COUNT(*) FROM tb_seckill_order WHERE product_id=3"
```

---

### ✅ 第五步：数据验证（5分钟）

#### 5.1 验证Redis数据
```bash
# 查看所有秒杀相关的key
redis-cli KEYS seckill:*

# 查看库存
redis-cli GET seckill:stock:1
redis-cli GET seckill:stock:2
redis-cli GET seckill:stock:3

# 查看用户秒杀记录
redis-cli GET seckill:user:1001:1
```

---

#### 5.2 验证MySQL数据
```sql
-- 登录MySQL
mysql -uroot -proot cloud_seckill

-- 1. 查看秒杀商品库存
SELECT id, product_name, stock FROM tb_seckill_product;

-- 2. 查看秒杀订单
SELECT order_no, user_id, product_id, product_name, status, create_time 
FROM tb_seckill_order 
ORDER BY create_time DESC 
LIMIT 10;

-- 3. 查看库存扣减日志
SELECT order_no, product_name, before_stock, after_stock, type, create_time 
FROM tb_stock_log 
ORDER BY create_time DESC 
LIMIT 10;

-- 4. 查看用户秒杀记录
SELECT user_id, product_id, order_no, create_time 
FROM tb_user_seckill 
ORDER BY create_time DESC 
LIMIT 10;

-- 5. 统计秒杀订单数量
SELECT product_name, 
       COUNT(*) AS 订单数, 
       SUM(CASE WHEN status=1 THEN 1 ELSE 0 END) AS 已支付,
       SUM(CASE WHEN status=0 THEN 1 ELSE 0 END) AS 待支付
FROM tb_seckill_order 
GROUP BY product_name;
```

---

### ✅ 第六步：性能测试（10分钟）

#### 6.1 使用JMeter压测
```bash
# 1. 打开JMeter
jmeter

# 2. 打开压测脚本
文件 → 打开 → 压测脚本.jmx

# 3. 配置压测参数
线程组 → 线程数：1000
线程组 → Ramp-Up时间：10秒
线程组 → 循环次数：10

# 4. 运行压测
运行 → 启动

# 5. 查看聚合报告
聚合报告 → 查看TPS、响应时间、错误率
```

**预期性能指标：**
| 指标 | 目标值 | 说明 |
|------|--------|------|
| TPS | 1000+ | 每秒处理请求数 |
| 平均响应时间 | <100ms | Redis扣库存很快 |
| 错误率 | <1% | 库存不足的正常失败 |
| 并发数 | 1000 | 同时在线用户数 |

---

#### 6.2 使用测试脚本.bat
```bash
# 双击运行
测试脚本.bat
```

**自动执行6个测试用例：**
1. ✅ 预热库存
2. ✅ 秒杀请求
3. ✅ 重复秒杀
4. ✅ 查询Redis库存
5. ✅ 查询MySQL订单数量
6. ✅ 查询MySQL库存

---

## 📊 测试结果记录

### 测试环境
- **操作系统：** Windows 11
- **Java版本：** 1.8.0_xxx
- **MySQL版本：** 5.7.x / 8.0.x
- **Redis版本：** 6.x / 7.x
- **RocketMQ版本：** 4.9.x

---

### 功能测试结果

| 测试项 | 预期结果 | 实际结果 | 状态 |
|--------|---------|---------|------|
| 预热库存 | 成功 | ✅ / ❌ | |
| 秒杀请求 | 成功 | ✅ / ❌ | |
| 重复秒杀 | 拒绝 | ✅ / ❌ | |
| 库存不足 | 拒绝 | ✅ / ❌ | |
| 并发秒杀 | 成功 | ✅ / ❌ | |
| Redis数据 | 正确 | ✅ / ❌ | |
| MySQL数据 | 正确 | ✅ / ❌ | |

---

### 性能测试结果

| 指标 | 目标值 | 实际值 | 状态 |
|------|--------|--------|------|
| TPS | 1000+ | ___ | ✅ / ❌ |
| 平均响应时间 | <100ms | ___ms | ✅ / ❌ |
| 错误率 | <1% | ___%  | ✅ / ❌ |
| 并发数 | 1000 | ___ | ✅ / ❌ |

---

## 🐛 常见问题排查

### 问题1：MySQL连接失败
**错误信息：**
```
Access denied for user 'root'@'localhost'
```

**解决方案：**
1. 检查MySQL密码是否正确
2. 修改 `application.yml` 中的密码
3. 重启服务

---

### 问题2：Redis连接失败
**错误信息：**
```
Unable to connect to Redis
```

**解决方案：**
1. 检查Redis是否启动：`redis-cli ping`
2. 检查Redis端口：6379
3. 检查防火墙设置

---

### 问题3：RocketMQ连接失败
**错误信息：**
```
No route info of this topic
```

**解决方案：**
1. 检查NameServer是否启动
2. 检查Broker是否启动
3. 检查 `application.yml` 中的 name-server 配置
4. 启动Broker时添加：`autoCreateTopicEnable=true`

---

### 问题4：Lua脚本执行失败
**错误信息：**
```
ERR Error running script
```

**解决方案：**
1. 检查 `stock.lua` 文件是否存在
2. 检查 `RedisConfig.java` 中的脚本路径
3. 重启 seckill-service

---

### 问题5：订单未创建
**现象：**
- Redis库存扣减成功
- 但MySQL没有订单记录

**解决方案：**
1. 检查 seckill-consumer 是否启动
2. 检查 RocketMQ 消息是否发送成功
3. 查看 consumer 日志是否有异常
4. 检查MySQL连接是否正常

---

### 问题6：库存不一致
**现象：**
- Redis库存与MySQL库存不一致

**解决方案：**
1. 重新预热库存：`POST /seckill/preload/{productId}`
2. 检查是否有消费失败的消息
3. 检查MySQL乐观锁是否生效
4. 查看 tb_stock_log 表的扣减记录

---

## ✅ 测试完成检查清单

### 功能完整性
- [ ] 预热库存功能正常
- [ ] 秒杀请求功能正常
- [ ] 防重复秒杀生效
- [ ] 防超卖机制生效
- [ ] 订单创建成功
- [ ] 库存扣减正确
- [ ] 日志记录完整

### 数据一致性
- [ ] Redis库存正确
- [ ] MySQL库存正确
- [ ] Redis与MySQL库存一致
- [ ] 订单数量正确
- [ ] 用户秒杀记录正确
- [ ] 库存日志完整

### 性能表现
- [ ] TPS达到目标
- [ ] 响应时间符合预期
- [ ] 错误率在可接受范围
- [ ] 并发处理正常
- [ ] 无内存泄漏
- [ ] 无死锁现象

---

## 📝 测试报告模板

### 测试概述
- **测试时间：** 2026-02-08
- **测试人员：** XXX
- **测试环境：** Windows 11 + Java 1.8 + MySQL 8.0 + Redis 7.0
- **测试目标：** 验证秒杀系统功能和性能

### 测试结果
- **功能测试：** 7/7 通过
- **性能测试：** TPS=1200, 响应时间=85ms, 错误率=0.5%
- **数据一致性：** 通过
- **总体评价：** ✅ 通过

### 发现的问题
1. 无

### 优化建议
1. 增加库存回滚机制
2. 增加限流功能
3. 增加监控面板

---

## 🎯 下一步计划

### 如果测试通过 ✅
1. 提交代码到Git
2. 编写技术文档
3. 准备技术分享
4. 优化和扩展功能

### 如果测试失败 ❌
1. 记录错误日志
2. 分析失败原因
3. 修复问题
4. 重新测试

---

**测试完成后，请填写测试结果并生成测试报告！** 📊
